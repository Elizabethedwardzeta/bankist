/* console.log(document.head);
console.log(document.body);

const header = document.querySelector('.header');
const allSections = document.querySelectorAll('secction');
console.log(header);

const section1 = document.getElementById('section--1');
section1.remove();
console.log(allSections);

const allButtons = document.getElementsByTagName('button');

//const btnNavEl = document.querySelector('nav--mobile-icon');
//btnNavEl.remove();
console.log(allButtons);
console.log(document.getElementsByClassName('btn'));
console.log(document.getElementsByTagName('button'));

//CREATING AND INSERTING ELEMENTS
const message = document.createElement('div');

// classlist
//classlist.add() Add an element to the existing classlist
//classlist.remove() remove an element from the existing classlist
//classlist.toggle() Toggle an element from the existing classlist
//classlist.contains() check if an element is in the existing classlist
 */
